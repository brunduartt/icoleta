import { ComponentFixture, TestBed } from '@angular/core/testing';
import { of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, Data } from '@angular/router';

import { IColetaTestModule } from '../../../test.module';
import { CollectPointComponent } from 'app/entities/collect-point/collect-point.component';
import { CollectPointService } from 'app/entities/collect-point/collect-point.service';
import { CollectPoint } from 'app/shared/model/collect-point.model';

describe('Component Tests', () => {
  describe('CollectPoint Management Component', () => {
    let comp: CollectPointComponent;
    let fixture: ComponentFixture<CollectPointComponent>;
    let service: CollectPointService;

    beforeEach(() => {
      TestBed.configureTestingModule({
        imports: [IColetaTestModule],
        declarations: [CollectPointComponent],
        providers: [
          {
            provide: ActivatedRoute,
            useValue: {
              data: {
                subscribe: (fn: (value: Data) => void) =>
                  fn({
                    pagingParams: {
                      predicate: 'id',
                      reverse: false,
                      page: 0
                    }
                  })
              }
            }
          }
        ]
      })
        .overrideTemplate(CollectPointComponent, '')
        .compileComponents();

      fixture = TestBed.createComponent(CollectPointComponent);
      comp = fixture.componentInstance;
      service = fixture.debugElement.injector.get(CollectPointService);
    });

    it('Should call load all on init', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new CollectPoint(123)],
            headers
          })
        )
      );

      // WHEN
      comp.ngOnInit();

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.collectPoints && comp.collectPoints[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should load a page', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new CollectPoint(123)],
            headers
          })
        )
      );

      // WHEN
      comp.loadPage(1);

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.collectPoints && comp.collectPoints[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should calculate the sort attribute for an id', () => {
      // WHEN
      comp.ngOnInit();
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['id,desc']);
    });

    it('should calculate the sort attribute for a non-id attribute', () => {
      // INIT
      comp.ngOnInit();

      // GIVEN
      comp.predicate = 'name';

      // WHEN
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['name,desc', 'id']);
    });
  });
});
